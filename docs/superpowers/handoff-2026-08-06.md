# Handoff — Google Drive Backend (ค้างไว้ 2026-08-06 ก่อนเที่ยงคืน)

เอกสารนี้สำหรับทำงานต่อพรุ่งนี้ — อ่านไฟล์นี้ก่อน แล้วค่อยอ่าน `.superpowers/sdd/progress.md` ประกอบ

## สถานะรวม

- Repo: `E:\ST\SillyTavern\public\scripts\extensions\third-party\st-tavernsync`
- Branch ที่ทำงาน: **`drive-backend`** (master สะอาด ไม่ได้แตะ)
- โหมดทำงาน: subagent-driven development — implementer ทีละ task + reviewer ตรวจทุก task
- เหตุที่หยุด: โควต้า Kimi หมดกลางการ dispatch implementer ของ Task 6

## ทำเสร็จแล้ว (5 tasks — ผ่านรีวิวทุกตัว)

| Task | Commit | สาระ |
|---|---|---|
| 1. StorageRevision + RemoteSnapshot | `fa78fc3` | interface ใหม่ `getSnapshot`/`putManifest(m, ifRevision)`, HTTP wire byte-identical |
| 2. BackendRuntime factory + SaltProvider | `7513b2c` | runtime.ts ใหม่, ย้าย blob crypto เข้า runtime, remembered key มี namespace |
| 3. HKDF subkeys + salt จาก folderId | `694eb28` | `src/crypto/subkeys.ts` — manifest-enc/blob-enc/blob-name |
| 4. Drive API client ระดับล่าง | `9ff9171` | `src/backend/drive/client.ts` — list/upload(multipart+resumable)/quota/searchRootFolders |
| 5. DriveAdapter blobs/quota/discovery | `8af2c5b` | `src/backend/drive/adapter.ts` — getSnapshot/putManifest ยังเป็น throw-stub รอ Task 6 |

เทสตอนหยุด: **39/39 ผ่าน**, `npx tsc --noEmit` สะอาด

## ค้างอยู่ตรงไหน

**Task 6 (manifest commits) — implementer เขียนแค่ไฟล์เทสไว้ก่อนโควต้าหมด:**
- มีไฟล์ untracked `src/backend/drive/__tests__/commits.test.ts` (126 บรรทัด, เทสจริงตาม brief ครบ + เทส adapter ระดับ ConflictError/fork) — **เก็บไว้ใช้ต่อได้เลย ไม่ต้องเขียนใหม่**
- ยังไม่มี `src/backend/drive/commits.ts` และ getSnapshot/putManifest ใน adapter.ts ยังเป็น stub
- brief ของ Task 6 อยู่ที่ `.superpowers/sdd/task-6-brief.md` (สร้างไว้แล้ว)

**สิ่งที่ต้องทำใน Task 6 (สำคัญสุด 2 ข้อ):**
1. **engine double-HMAC fix** — engine.ts ~565-566 และ ~665-666 ยังเรียก `rt.crypto.blobNameFor()` ก่อนคุย adapter อยู่ ต้องลบออกให้ engine ส่ง logical hash ตรง ๆ (HTTP ไม่กระทบเพราะ blobNameFor เป็น identity)
2. สร้าง `commits.ts` (parseCommitMeta/computeHeads/revisionOfHeads/findCommonAncestor) + implement getSnapshot (single/fork) + putManifest (revision check + rollup commits เมื่อ heads>4) ตาม task-6-brief.md — ระวังบั๊กในโค้ดตัวอย่างของ brief ตรง rollup loop (id ปนกันระหว่าง file id กับ commit id)

**Task ที่เหลือหลัง Task 6:**
- Task 7: engine fork merge 3 ทาง (`src/sync-core/merge.ts` + runSync รับ fork snapshot — brief อยู่ใน plan)
- Task 8: GIS OAuth (`src/backend/drive/oauth.ts`) + Drive runtime wiring ใน factory + settings fields (`driveClientId`, `driveFolderId`, backendMode 'drive')
- Task 9: Panel UI (เลือก backend/ปุ่ม Connect/Storage line) + manual GC (`src/backend/drive/gc.ts`) — อย่าลืม index.ts:146 ที่ยัง new HttpStorageAdapter ตรง ๆ (ค้างจาก Task 2)
- Task 10: bump version 0.2.0 (3 จุด: package.json, settings.ts BUILD_ID, manifest.json) + docs/google-drive-setup.md + manual E2E
- ปิดท้าย: final whole-branch review แล้วค่อย merge เข้า master

## วิธีทำต่อพรุ่งนี้

1. `cd E:\ST\SillyTavern\public\scripts\extensions\third-party\st-tavernsync` แล้ว `git checkout drive-backend` (ถ้ายังไม่ได้อยู่)
2. อ่าน ledger: `.superpowers/sdd/progress.md`
3. dispatch implementer ของ Task 6 ใหม่ด้วย prompt เดิม + แจ้งว่า `commits.test.ts` มีอยู่แล้วให้ใช้ต่อ (อย่าเขียนทับถ้าไม่จำเป็น)
4. ทุก task เสร็จต้อง review ด้วย `review-package` ตาม ritual เดิม
5. plan แม่: `docs/superpowers/plans/2026-08-06-google-drive-backend.md` / spec แม่: `docs/superpowers/specs/2026-08-06-google-drive-backend-design.md`

## ข้อควรจำ (ห้ามลืม)

- HTTP/Worker backend เดิมต้อง byte-identical เสมอ — มีเทส regression คุมแล้ว อย่าแกะมัน
- E2EE บังคับสำหรับ Drive; manifest เข้ารหัสทั้งก้อน; ชื่อ blob = HMAC
- ห้ามตัดสิน fork ด้วย mtime — conflict เข้า UI เดิมเท่านั้น
- `package-lock.json` มี diff ค้างจาก npm install — เจตนาไม่ commit
- เมื่อ deploy จริง: ผู้ใช้ต้องสร้าง Google Cloud OAuth Client ID เอง และใช้ตัวเดียวกันทุกเครื่อง
